[
  {
    "objectID": "about.html",
    "href": "about.html",
    "title": "About Me",
    "section": "",
    "text": "My name is Logan Foster, I am an Echols’ Scholar and 3rd-year student of Mathematics at the University of Virginia (UVA). I also study Russian and will soon being doing research with Dr. Hadi Daneshemand into diffusion models and language transformers.\nI currently work as a Machine Learning Engineer at True Path Vision, where I build and deploy computer vision models to provide security and management services to hotels. I have built and trained state of the art computer vision models to detect guns and other waepons, and I am currently building methods to automate the generation and collection of additional training data. Among these methods include effecient and distributed web harvesters and automated blender-based 3D object rendering pipelines.\nI was one of six students to recieve the 2026 Distinguished Undergraduate Award from the Institute of World Languages at UVA for my excelence in learning Russian. I am also an Echols’ scholar at UVA, and honor awared to only the top five percent of students in the College of Arts and Sciences. I am also a two-time recipient of the Chick-Fil-A Remarkable Futures Scholarship.\nIn my free time, I love to serve at my church, travel, hike the Alps, DJ, play nerdy board games, and spend time with friends."
  },
  {
    "objectID": "posts/welcome/index.html",
    "href": "posts/welcome/index.html",
    "title": "Welcome To My Blog",
    "section": "",
    "text": "This is the first post in a Quarto blog. Welcome!\n\nSince this post doesn’t specify an explicit image, the first image in the post will be used in the listing page of posts."
  },
  {
    "objectID": "posts/post-with-code/index.html",
    "href": "posts/post-with-code/index.html",
    "title": "Post With Code",
    "section": "",
    "text": "This is a post with executable code."
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "Logan’s Site",
    "section": "",
    "text": "Understanding the Normal Equation\n\n\n\nmath\n\n\n\n\n\n\n\n\n\nMay 13, 2026\n\n\nLogan Foster\n\n\n\n\n\n\n\n\n\n\n\n\nPost With Code\n\n\n\nnews\n\ncode\n\nanalysis\n\n\n\n\n\n\n\n\n\nMay 10, 2026\n\n\nHarlow Malloc\n\n\n\n\n\n\n\n\n\n\n\n\nWelcome To My Blog\n\n\n\nnews\n\n\n\n\n\n\n\n\n\nMay 7, 2026\n\n\nTristan O’Malley\n\n\n\n\n\nNo matching items"
  },
  {
    "objectID": "posts/normal-equation/index.html",
    "href": "posts/normal-equation/index.html",
    "title": "Understanding the Normal Equation",
    "section": "",
    "text": "Linear regression is one of the most fundamental models in machine learning. In fact, least squares linear regression, commonly called “ordinary least squares” or “OLS”, was first discovered by Isaac Newton over 250 years before the term “machine learning” was coined!1\n\n\nLinear regression works by fitting a high-dimensional “plane” to a dataset. This is easiest to visualize in the case where our features (inputs) are one-dimensional:\n\n\nCode\nfrom sklearn.datasets import make_regression\nimport numpy as np\n\nX, y = make_regression(n_samples = 50, n_features= 1, n_informative=1, n_targets=1, bias=2, noise = 10, random_state=42)\n\n# Doing regression\n\n## add bias term\nX_b = np.hstack((np.ones(X.shape), X))\n## use the normal equation to find the best fit\n\ntheta = np.linalg.solve((X_b.T @ X_b), (X_b.T @ y))\n\nX_line = np.linspace(X.min(), X.max(), 100)\ny_line = X_line * theta[1] + theta[0]\n\n\n## plot results\nimport plotly.graph_objects as go\n\nfig = go.Figure()\nfig.add_trace(go.Scatter(x=X.flatten(), y=y, mode='markers', name='random data'))\nfig.add_trace(go.Scatter(x=X_line, y=y_line, mode='lines', name='best fiting line'))\n\nfig.update_layout(\n    title=dict(text=\"Simple 2-D Linear Regression\", x=0.5, xanchor=\"center\"),\n    yaxis=dict(showticklabels=False),\n    margin=dict(l=20, r=20)\n)\nfig.show()\n\n\n                            \n                                            \n\n\nWe can also visualize regression in three dimensions (so, two dimensions for features, since the third is the value we are trying to predict).\n\n\nCode\nX, y = make_regression(n_samples = 100, n_features= 2, n_informative=2, n_targets=1, bias=2, noise = 30, random_state=42)\n\n# Doing regression\n\n## add bias term\nX_b = np.hstack((np.ones((X.shape[0], 1)), X))\n## use the normal equation to find the best fit\n\ntheta = np.linalg.solve((X_b.T @ X_b), (X_b.T @ y))\n\n\nax_1 = np.linspace(X[:, 0].min(), X[:, 0].max(), 100)\nax_2 = np.linspace(X[:, 1].min(), X[:, 1].max(), 100)\nX_plane = np.array(np.meshgrid(ax_1, ax_2, indexing='xy'))\n\ny_plane = theta[0] + theta[1] * X_plane[0] + theta[2] * X_plane[1]\n\n\n## plot results\nfig = go.Figure()\nfig.add_trace(go.Scatter3d(x=X[:, 0], y=X[:, 1], z=y, mode='markers', name='random data',\n    marker=dict(size=5)))\nfig.add_trace(go.Surface(x=ax_1, y=ax_2, z=y_plane, opacity=0.5, showlegend=False, showscale=False))\nfig.update_layout(\n    title=dict(text=\"Simple 3-D Linear Regression\", x=0.5, xanchor=\"center\"),\n    margin=dict(t=40, b=40, l=0, r=0),\n    scene=dict(\n        zaxis=dict(showticklabels=False, title=\"\", showline=False, zeroline=False),\n        camera=dict(\n            eye=dict(x=0.9, y=-2.0, z=1.4),\n            center=dict(x=0, y=0, z=-0.15)\n        )\n    )\n)\nfig.show()\n\n\n                            \n                                            \n\n\nIn this example, we define the “line (or plane) of best fit” as the one which minimizes the mean of the squared distance between our prediction and the data at all points. This is called “mean squared error” or “MSE”. MSE is calculated as \n\\mathrm{MSE} = \\frac{1}{n} \\sum_{i = 1}^{n} (y_i - \\hat{y}_i)^2,\n\nwhere y_i is the true value of the i-th sample of our dataset, and \\hat{y}_i is our prediction of the true value using only the i-th set of features x_i.\nUsually, we find the parameters x describing the line of best fit through some form of gradient descent. But in cases where our dataset is small enough, we can use a closed form solution to find the optimal parameters. This closed form solution looks like solving the linear equation\n\nA^T A x = A^T y.\n\nThis equation is called the normal equation, and the rest of this blog is devoted to understanding why it works!\n\n\n\nWe can represent our plane of best fit as a linear equation:\n\nAx = \\hat{y},\n\nwhere A is an n by d matrix (n are the number of data points we have, d represents the number of features our dataset has), x is a vector representing our parameters, and \\hat{y} is our prediction. Our goal is to find the x which minimizes the MSE between our predictions and our true labels. Lets rewrite MSE:\n\n\\mathrm{MSE}\n= \\frac{1}{n} \\sum_{i = 1}^{n} (y_i - \\hat{y}_i)^2\n= \\frac{1}{n} \\|A x - y\\|^2 = \\frac{1}{n} (A x - y)^T (A x - y).\n\nSince \\frac{1}{n} is a positive constant, we can ignore it when trying to finding the miminum of or error term: the minimizing input of a function f is the same as the minimizing input of cf where c is positive.\nSo the parameters x which minimize the MSE also minimize f(x) = (A x - y)^T (A x - y).\n\nf(x) = (A x - y)^T (A x - y) = x^T A^T A x - x^T A^T y - y^T A x + y^T y.\n\nThe minimum of this function occurs when the gradient is zero (this is because MSE with linear regression is convex, a blog post about convexity and why it is important is soon to follow!). Taking the gradient of f with respect to x yeilds\n\n\\nabla f(x)\n= \\nabla \\left(x^T A^T A x\\right)\n- \\nabla \\left(x^T A^T y\\right)\n- \\nabla \\left(y^T A x\\right).\n\nNotice that y^T y is dropped since it doesn’t depend on x: the derivatives of constants are always 0.\n\n\nNow we need to evaluate \\nabla \\left(x^T A^T A x\\right), \\nabla \\left(x^T A^T y\\right), and \\nabla \\left(y^T A x\\right). A lot of tedious math is to follow. If that doens’t interest you, feel free to skip here [INSERT SKIP].\n\n\nStarting with \\nabla \\left(x^T A^T A x\\right). Let S = A^T A. Since A is n \\times d, S is d \\times d. S is also symmetric, which will help us later: \n\\begin{aligned}\nS_{ij} & = (A^T A)_{ij} = A^T_{i \\cdot} \\cdot A_{\\cdot j} = \\sum_{k = 1}^{n} (A^T)_{ik} A_{kj} = \\sum_{k = 1}^{n} A_{ki} A_{kj} \\\\\n&= A_{\\cdot i} \\cdot A_{\\cdot j} = A_{\\cdot j} \\cdot A_{\\cdot i} \\\\\n&= \\sum_{k = 1}^{n} A_{kj} A_{ki} = A^T_{j \\cdot} \\cdot A_{\\cdot i} = (A^T A)_{ji} = S_{ji}.\n\\end{aligned}\n\nSo x^T A^T A x = x^T S x, which we can write as\n\nx^T S x\n=\nx^T\n\\begin{bmatrix}\n\\sum_{i = 1}^{d} S_{1i} x_i \\\\\n\\sum_{i = 1}^{d} S_{2i} x_i \\\\\n\\vdots \\\\\n\\sum_{i = 1}^{d} S_{di} x_i\n\\end{bmatrix}\n=\nx_1 \\sum_{i = 1}^{d} S_{1i} x_i\n+\nx_2 \\sum_{i = 1}^{d} S_{2i} x_i\n+\n\\cdots\n+\nx_d \\sum_{i = 1}^{d} S_{di} x_i.\n\nLets consider the partial derivative of the above with respect to x_k: \n\\begin{aligned}\n\\frac{\\partial}{\\partial x_k} x^T S x\n&= \\frac{\\partial}{\\partial x_k}\n\\left(\nx_1 \\sum_{i=1}^{d} S_{1i} x_i\n+ x_2 \\sum_{i=1}^{d} S_{2i} x_i\n+ \\cdots\n+ x_d \\sum_{i=1}^{d} S_{di} x_i\n\\right) \\\\\n&= x_1 S_{1k}\n+ x_2 S_{2k}\n+ \\cdots\n+ \\sum_{i=1}^{d} S_{ki} x_i\n+ x_k S_{kk}\n+ \\cdots\n+ x_d S_{dk}.\n\\end{aligned}\n\nRemembering that S is symmetric means that S_ki = S_ik, so\n$$\n\\begin{aligned}\n\\frac{\\partial}{\\partial x_k} x^T S x\n\n&= x_1 S_{1k}\n+ x_2 S_{2k}\n+ \\cdots\n+ \\sum_{i=1}^{d} S_{ki} x_i\n+ x_k S_{kk}\n+ \\cdots\n+ x_d S_{dk}.\n\\end{aligned}\n\\\n&= S_{k1} x_1 + [claude finish here] \\\n&=\n$$"
  },
  {
    "objectID": "posts/normal-equation/index.html#linear-regression",
    "href": "posts/normal-equation/index.html#linear-regression",
    "title": "Understanding the Normal Equation",
    "section": "",
    "text": "Linear regression works by fitting a high-dimensional “plane” to a dataset. This is easiest to visualize in the case where our features (inputs) are one-dimensional:\n\n\nCode\nfrom sklearn.datasets import make_regression\nimport numpy as np\n\nX, y = make_regression(n_samples = 50, n_features= 1, n_informative=1, n_targets=1, bias=2, noise = 10, random_state=42)\n\n# Doing regression\n\n## add bias term\nX_b = np.hstack((np.ones(X.shape), X))\n## use the normal equation to find the best fit\n\ntheta = np.linalg.solve((X_b.T @ X_b), (X_b.T @ y))\n\nX_line = np.linspace(X.min(), X.max(), 100)\ny_line = X_line * theta[1] + theta[0]\n\n\n## plot results\nimport plotly.graph_objects as go\n\nfig = go.Figure()\nfig.add_trace(go.Scatter(x=X.flatten(), y=y, mode='markers', name='random data'))\nfig.add_trace(go.Scatter(x=X_line, y=y_line, mode='lines', name='best fiting line'))\n\nfig.update_layout(\n    title=dict(text=\"Simple 2-D Linear Regression\", x=0.5, xanchor=\"center\"),\n    yaxis=dict(showticklabels=False),\n    margin=dict(l=20, r=20)\n)\nfig.show()\n\n\n                            \n                                            \n\n\nWe can also visualize regression in three dimensions (so, two dimensions for features, since the third is the value we are trying to predict).\n\n\nCode\nX, y = make_regression(n_samples = 100, n_features= 2, n_informative=2, n_targets=1, bias=2, noise = 30, random_state=42)\n\n# Doing regression\n\n## add bias term\nX_b = np.hstack((np.ones((X.shape[0], 1)), X))\n## use the normal equation to find the best fit\n\ntheta = np.linalg.solve((X_b.T @ X_b), (X_b.T @ y))\n\n\nax_1 = np.linspace(X[:, 0].min(), X[:, 0].max(), 100)\nax_2 = np.linspace(X[:, 1].min(), X[:, 1].max(), 100)\nX_plane = np.array(np.meshgrid(ax_1, ax_2, indexing='xy'))\n\ny_plane = theta[0] + theta[1] * X_plane[0] + theta[2] * X_plane[1]\n\n\n## plot results\nfig = go.Figure()\nfig.add_trace(go.Scatter3d(x=X[:, 0], y=X[:, 1], z=y, mode='markers', name='random data',\n    marker=dict(size=5)))\nfig.add_trace(go.Surface(x=ax_1, y=ax_2, z=y_plane, opacity=0.5, showlegend=False, showscale=False))\nfig.update_layout(\n    title=dict(text=\"Simple 3-D Linear Regression\", x=0.5, xanchor=\"center\"),\n    margin=dict(t=40, b=40, l=0, r=0),\n    scene=dict(\n        zaxis=dict(showticklabels=False, title=\"\", showline=False, zeroline=False),\n        camera=dict(\n            eye=dict(x=0.9, y=-2.0, z=1.4),\n            center=dict(x=0, y=0, z=-0.15)\n        )\n    )\n)\nfig.show()\n\n\n                            \n                                            \n\n\nIn this example, we define the “line (or plane) of best fit” as the one which minimizes the mean of the squared distance between our prediction and the data at all points. This is called “mean squared error” or “MSE”. MSE is calculated as \n\\mathrm{MSE} = \\frac{1}{n} \\sum_{i = 1}^{n} (y_i - \\hat{y}_i)^2,\n\nwhere y_i is the true value of the i-th sample of our dataset, and \\hat{y}_i is our prediction of the true value using only the i-th set of features x_i.\nUsually, we find the parameters x describing the line of best fit through some form of gradient descent. But in cases where our dataset is small enough, we can use a closed form solution to find the optimal parameters. This closed form solution looks like solving the linear equation\n\nA^T A x = A^T y.\n\nThis equation is called the normal equation, and the rest of this blog is devoted to understanding why it works!"
  },
  {
    "objectID": "posts/normal-equation/index.html#footnotes",
    "href": "posts/normal-equation/index.html#footnotes",
    "title": "Understanding the Normal Equation",
    "section": "Footnotes",
    "text": "Footnotes\n\n\nSee Least squares: History and Machine learning: History.↩︎"
  },
  {
    "objectID": "posts/normal-equation/index.html#the-normal-equation",
    "href": "posts/normal-equation/index.html#the-normal-equation",
    "title": "Understanding the Normal Equation",
    "section": "",
    "text": "We can represent our plane of best fit as a linear equation:\n\nAx = \\hat{y},\n\nwhere A is an n by d matrix (n are the number of data points we have, d represents the number of features our dataset has), x is a vector representing our parameters, and \\hat{y} is our prediction. Our goal is to find the x which minimizes the MSE between our predictions and our true labels. Lets rewrite MSE:\n\n\\mathrm{MSE}\n= \\frac{1}{n} \\sum_{i = 1}^{n} (y_i - \\hat{y}_i)^2\n= \\frac{1}{n} \\|A x - y\\|^2 = \\frac{1}{n} (A x - y)^T (A x - y).\n\nSince \\frac{1}{n} is a positive constant, we can ignore it when trying to finding the miminum of or error term: the minimizing input of a function f is the same as the minimizing input of cf where c is positive.\nSo the parameters x which minimize the MSE also minimize f(x) = (A x - y)^T (A x - y).\n\nf(x) = (A x - y)^T (A x - y) = x^T A^T A x - x^T A^T y - y^T A x + y^T y.\n\nThe minimum of this function occurs when the gradient is zero (this is because MSE with linear regression is convex, a blog post about convexity and why it is important is soon to follow!). Taking the gradient of f with respect to x yeilds\n\n\\nabla f(x)\n= \\nabla \\left(x^T A^T A x\\right)\n- \\nabla \\left(x^T A^T y\\right)\n- \\nabla \\left(y^T A x\\right).\n\nNotice that y^T y is dropped since it doesn’t depend on x: the derivatives of constants are always 0.\n\n\nNow we need to evaluate \\nabla \\left(x^T A^T A x\\right), \\nabla \\left(x^T A^T y\\right), and \\nabla \\left(y^T A x\\right). A lot of tedious math is to follow. If that doens’t interest you, feel free to skip here [INSERT SKIP].\n\n\nStarting with \\nabla \\left(x^T A^T A x\\right). Let S = A^T A. Since A is n \\times d, S is d \\times d. S is also symmetric, which will help us later: \n\\begin{aligned}\nS_{ij} & = (A^T A)_{ij} = A^T_{i \\cdot} \\cdot A_{\\cdot j} = \\sum_{k = 1}^{n} (A^T)_{ik} A_{kj} = \\sum_{k = 1}^{n} A_{ki} A_{kj} \\\\\n&= A_{\\cdot i} \\cdot A_{\\cdot j} = A_{\\cdot j} \\cdot A_{\\cdot i} \\\\\n&= \\sum_{k = 1}^{n} A_{kj} A_{ki} = A^T_{j \\cdot} \\cdot A_{\\cdot i} = (A^T A)_{ji} = S_{ji}.\n\\end{aligned}\n\nSo x^T A^T A x = x^T S x, which we can write as\n\nx^T S x\n=\nx^T\n\\begin{bmatrix}\n\\sum_{i = 1}^{d} S_{1i} x_i \\\\\n\\sum_{i = 1}^{d} S_{2i} x_i \\\\\n\\vdots \\\\\n\\sum_{i = 1}^{d} S_{di} x_i\n\\end{bmatrix}\n=\nx_1 \\sum_{i = 1}^{d} S_{1i} x_i\n+\nx_2 \\sum_{i = 1}^{d} S_{2i} x_i\n+\n\\cdots\n+\nx_d \\sum_{i = 1}^{d} S_{di} x_i.\n\nLets consider the partial derivative of the above with respect to x_k: \n\\begin{aligned}\n\\frac{\\partial}{\\partial x_k} x^T S x\n&= \\frac{\\partial}{\\partial x_k}\n\\left(\nx_1 \\sum_{i=1}^{d} S_{1i} x_i\n+ x_2 \\sum_{i=1}^{d} S_{2i} x_i\n+ \\cdots\n+ x_d \\sum_{i=1}^{d} S_{di} x_i\n\\right) \\\\\n&= x_1 S_{1k}\n+ x_2 S_{2k}\n+ \\cdots\n+ \\sum_{i=1}^{d} S_{ki} x_i\n+ x_k S_{kk}\n+ \\cdots\n+ x_d S_{dk}.\n\\end{aligned}\n\nRemembering that S is symmetric means that S_ki = S_ik, so\n$$\n\\begin{aligned}\n\\frac{\\partial}{\\partial x_k} x^T S x\n\n&= x_1 S_{1k}\n+ x_2 S_{2k}\n+ \\cdots\n+ \\sum_{i=1}^{d} S_{ki} x_i\n+ x_k S_{kk}\n+ \\cdots\n+ x_d S_{dk}.\n\\end{aligned}\n\\\n&= S_{k1} x_1 + [claude finish here] \\\n&=\n$$"
  }
]